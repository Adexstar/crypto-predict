-- Baseline migration: captures current schema state
-- This is a placeholder since schema already exists in production
-- It marks the starting point for future migrations

-- Do nothing - schema already created by prisma db push
SELECT 1;
