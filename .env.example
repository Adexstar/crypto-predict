# Environment Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# Admin Credentials (change these!)
VITE_ADMIN_EMAIL=admin@example.com
VITE_ADMIN_PASSWORD=change_this_password

# System Configuration
VITE_SYSTEM_MODE=SIMULATION_ONLY

# Default User Password (for demo accounts)
VITE_DEFAULT_USER_PASSWORD=change_this_password

# Admin Panel Password
VITE_ADMIN_PANEL_PASSWORD=change_this_password

# Security Note:
# For production, implement proper backend authentication
# Client-side credentials are NOT secure
# Consider using: Auth0, Supabase, Firebase Auth, or custom backend
