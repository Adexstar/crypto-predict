# Environment Configuration Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env or .env.local to version control!

# ⚠️ BACKEND ADMIN CREDENTIALS (SECURE - Server Side Only)
# These are validated server-side and NEVER exposed to frontend
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_this_to_secure_password

# JWT Secret (minimum 32 characters for security)
JWT_SECRET=change_this_to_a_very_long_random_secret_at_least_32_characters

# Database Connection
DATABASE_URL=your_database_url_here

# Email Service Configuration
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your_app_specific_password

# URLs
FRONTEND_URL=http://localhost:5173
NODE_ENV=development

# SECURITY NOTES:
# - Admin credentials are now backend-only (NOT exposed to frontend)
# - Passwords are hashed and verified server-side only
# - For Vercel, set ADMIN_EMAIL and ADMIN_PASSWORD (without VITE_ prefix)
# - Never use VITE_ prefix for sensitive credentials
# Consider using: Auth0, Supabase, Firebase Auth, or custom backend
