[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["cd backend && npm install --omit=dev"]

[phases.build]
cmds = [
  "cd backend && npx prisma generate"
]

[start]
cmd = "cd backend && npx prisma db push --accept-data-loss --skip-generate && node server.js"
